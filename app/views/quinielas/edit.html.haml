%h1= Quiniela.model_name.human

%p
  %b= Quiniela.human_attribute_name(:name)
  = @quiniela.name
%p
  %b= Quiniela.human_attribute_name(:start_at)
  = @quiniela.start_at
%p
  %b= Quiniela.human_attribute_name(:end_at)
  = @quiniela.end_at
%p
  %b= Quiniela.human_attribute_name(:cost)
  = @quiniela.cost

%h1= Game.model_name.human(count: 2)

%h1= t('app.sign_up')

= form_for @quiniela do |f|
  = f.fields_for :choices do |game|
    .field
      = game.label :local
      = game.check_box :local
    .field
      = game.label :draw
      = game.check_box :draw
    .field
      = game.label :visitor
      = game.check_box :visitor
  .fields
    = f.collection_check_boxes(:user_ids, User.all, :id, :username)
  .actions
    = f.submit t('links.save')

