%h1= Team.model_name.human(count: 2)
%hr
%table.quiniela-table
  %tr
    %th= Team.human_attribute_name(:name)
    %th= t('links.show')
    %th= t('links.edit')
    %th= t('links.destroy')

  - @teams.each do |team|
    %tr
      %td= team.name
      %td= link_to t('links.show'), [:admin, team]
      %td= link_to t('links.edit'), edit_admin_team_path(team)
      %td= link_to t('links.destroy'), admin_team_path(team), method: :delete, data: { confirm: t('buttons.confirm') } 
%br
%hr
.button.radius.round
  = link_to t('links.model.new', model: Team.model_name.human ), new_admin_team_path
