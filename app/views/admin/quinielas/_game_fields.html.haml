.nested-fields
  .field
    = f.label :local
    = f.collection_select(:local, Team.all, :name, :name)
  .field
    = f.label :visitor
    = f.collection_select(:visitor, Team.all, :name, :name)
  .field
    = f.label :result
    = f.select(:result, options_for_select([:nil, :empate, :local, :visitante]))
  = link_to_remove_association t('app.remove_game'), f
